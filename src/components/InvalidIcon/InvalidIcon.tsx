import { WarningIcon } from "@phosphor-icons/react";
import { styled } from "../../utils";
import styles from "./InvalidIcon.module.css";

export const InvalidIcon = styled(WarningIcon, styles.InvalidIcon);
